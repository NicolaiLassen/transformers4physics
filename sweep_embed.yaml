program: train.py
name: embedding_sweep
entity: transformers4physics
project: v1
method: random
metric:
  goal: minimize
  name: loss/val
parameters:
  embedding__backbone: 
    values: ['conv', 'att']
  embedding__observable_net:
    values: ["()", "()"]
  embedding__recovery_net:
    values: ["()", "()"]
  
  learning__sched: 
    values: ['multistep', 'cosine']
  learning__learning_rate: 
    min: 0.00001
    max: 0.01
  opt__name:
    values: ["adamw"]
  
command:
  - ${args_no_hyphens}